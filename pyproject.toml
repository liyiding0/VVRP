[project]
name = "VVRP"
version = "0.1.0"
description = "Virtual Versatile Routing Platform."
requires-python = ">=3.11"
dependencies = [
    "prompt_toolkit>=3.0",
    "psutil>=5.9",
]

[project.scripts]
ccmd = "VVRP.CCmd.__main__:main"

[tool.setuptools.packages.find]
include = ["VVRP*"]
